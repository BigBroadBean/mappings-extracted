# MC 多版本反混淆项目 · 全链路文档

> 本文是 `mappings-extracted`（MC 多版本反混淆数据）的**全链路文档**：
> 不只讲数据本身，还讲清楚这份数据如何一路流转——
> **提取 → 生成 → 编译 → 注入 → 上报 → 消费**，以及每个环节的真实结论与踩坑记录。
>
> 配套文档：
> - 数据格式细节 → 本仓库 `README.md`
> - 下游工具包（生成器 + DLL + 验证工具 + 消费协议）→ `D:\VibeCoding\MCCombatStatusToolkit\docs\`
> - DLL 调试历程与避坑清单 → `D:\VibeCoding\MCCombatStatusJni\`《技术文档.md》《避坑指南.md》

---

## 〇、项目定位

`mappings-extracted` 是整条链路的**数据源头**：收录 Minecraft Java 版
**1.8.8 ~ 1.21.11 共 54 个版本**的反混淆数据，每个版本含最多 **6 个命名空间**，
总计约 **29.9 万类 / 127 万字段 / 202 万方法 / 1081 万参数（约 1.7GB）**。

它的价值：**让"查一个混淆名在任意版本/任意命名空间叫什么"变成一条 grep /
一次 CSV 查询**，把下游工具从"逐个版本手工查 lzma/mcp_config"中解放出来。

## 一、全链路总览

```
┌───────────────────────── 数据来源 ─────────────────────────┐
│ takenaka 反混淆网站 (53 版本)      官方 MCP 数据 (1.8.9)      │
│   D:\mappings-gh-pages\extract_maps.py  convert_mcp_189.py │
└────────────────────────────┬───────────────────────────────┘
                             ▼
        ┌─────────────────────────────────────────┐
        │  ① 数据层  mappings-extracted (本仓库)    │
        │  54 版本 × 6 命名空间                     │
        │  classes.csv / fields.csv / methods.csv  │
        │  / params.csv / joined.srg / joined.exc  │
        └────────────────────┬────────────────────┘
                             ▼
        ┌─────────────────────────────────────────┐
        │  ② 生成层  gen_maps.py                   │
        │  按稳定名解析 31 个成员 → 171 张 JniMap   │
        │  → mc_maps_generated.h (C++ 表)          │
        │  (MCCombatStatusJni\tools\ / Toolkit\generator\) │
        └────────────────────┬────────────────────┘
                             ▼
        ┌─────────────────────────────────────────┐
        │  ③ 注入层  MCCombatStatusJni.dll         │
        │  injector.exe 注入 → JNI 附加 JVM        │
        │  版本检测(classpath) → 反射解析 → 判定    │
        └────────────────────┬────────────────────┘
                             ▼
        ┌─────────────────────────────────────────┐
        │  ④ 上报层  每 5ms                         │
        │  共享内存 Local\MCCombatStatus_<pid>     │
        │  UDP 127.0.0.1:35785 (2 字节)            │
        └────────────────────┬────────────────────┘
                             ▼
        ┌─────────────────────────────────────────┐
        │  ⑤ 消费层  AutoClicker 连点器 / 任意程序   │
        │  canAttack=1 才左键连点 / canPlace=1 才右键 │
        └─────────────────────────────────────────┘
```

| 环节 | 项目/文件 | 说明 |
|---|---|---|
| 数据来源 | `D:\mappings-gh-pages\` | 提取脚本（takenaka 网站爬取 + MCP 转换） |
| ① 数据层 | **`D:\VibeCoding\mappings-extracted\`（本仓库）** | 54 版本结构化数据 |
| ② 生成层 | `gen_maps.py`（两处副本） | 数据 → 171 张 C++ 映射表 |
| ③④ 注入上报 | `D:\VibeCoding\MCCombatStatusJni\` | DLL + 注入器 |
| ②③④ 打包 | `D:\VibeCoding\MCCombatStatusToolkit\` | 全链路工具包（含四篇文档） |
| ⑤ 消费层 | `D:\VibeCoding\AutoClicker-main\` | 连点器（DLL 已内嵌） |

---

## 二、数据层：mappings-extracted

### 2.1 数据来源

| 来源 | 覆盖 | 提取工具 |
|---|---|---|
| [takenaka](https://github.com/zlataovce/takenaka) 反混淆网站 | 53 个版本（1.8.8 ~ 1.21.11，缺 1.8.9） | `D:\mappings-gh-pages\extract_mappings.py` |
| 官方 MCP 数据 | 1.8.9（网站不收录） | `D:\mappings-gh-pages\convert_mcp_189.py` |

> 1.8.9 与网站版本的差异（fields/methods CSV 多 `side/desc` 两列、Spigot 列存 MCP
> 可读名、joined.srg 含 PK 行等）详见 `README.md`「1.8.9 特殊说明」。

### 2.2 六个命名空间

同一份 MC 代码，在不同视角下有 6 套名字：

| 命名空间 | 含义 | 覆盖 | 下游用途 |
|---|---|---|---|
| 混淆的 | 官方 jar 真实混淆名（`ave`/`enn`） | 全部 54 版 | **vanilla 表**（原版启动器） |
| Searge | MCP/SRG：1.8~1.16.5 是 `func_/field_`，1.17+ 是 stable `m_/f_` | 全部 | **forge 表**（Forge 1.8~1.20.1） |
| Spigot | CraftBukkit 名（1.8.9 该列存 MCP 可读名） | 全部 | 备用参考 |
| Mojang | 官方映射（`getInstance`） | 1.14.4+ | **mojang 表**（NeoForge 1.20.2+） |
| Yarn | Fabric Yarn | 1.14.4+ | 备用参考 |
| Intermediary | Fabric 运行时（`class_310`/`method_1551`） | 1.14+ | **intermediary 表**（Fabric） |

**核心规律**（真机实测）：类名和成员名是**两套独立演变的体系**。
磁盘上永远是混淆名；运行时被各加载器转换成"开发映射的名字"。
详见第七章。

### 2.3 每版本文件格式

| 文件 | 格式 | 关键点 |
|---|---|---|
| `classes.csv` | 6 列 = 6 命名空间类名 | 内嵌类用 `$`；该版本没有的命名空间留空 |
| `fields.csv` | `owner_obf` + 6 命名空间 + `kind` + `type` | 含枚举常量；1.8.9 多 `side/desc` 两列 |
| `methods.csv` | `owner_obf` + 6 命名空间 + `kind` + `signature` + `descriptor` + `return_type` | `descriptor` 是重建的 JVM 描述符（混淆命名空间类型名）；命名空间列是 `name(args)` 形式 |
| `params.csv` | 参数名长格式 | 旧版本（≤1.13）几乎只有 `argN` 占位；1.8.9 有 MCP 真实参数名 |
| `joined.srg` | `CL:/FD:/MD:` 行（obf → Searge） | 与官方 MCP joined 格式兼容；两侧描述符分属混淆/Searge 命名空间 |
| `joined.exc` | MCP 参数名（尽力而为） | 旧版本几乎为空 |

### 2.4 已知限制（数据侧）

1. 网站版本（除 1.8.9）无 MCP 风格参数名、无 `side/desc`
2. `access$` 合成方法缺失（网站不收录，约 280 条，仅 1.8.9 参考文件有）
3. 描述符重建失败率现代版本约 0.02%（网站渲染缺陷，`desc_fail` 字段记录）

---

## 三、生成层：gen_maps.py

`gen_maps.py` 是数据与 DLL 之间的桥梁，存在于两处（内容同步）：
`MCCombatStatusJni\tools\` 与 `MCCombatStatusToolkit\generator\`。

### 3.1 数据 → 171 张 JniMap 表

生成器按每个版本 × 命名空间，把 DLL 判定所需的 **31 个成员**
（getMinecraft / player / hitResult / typeOfHit / entityHit / ENTITY 枚举 /
isAlive / canAttackWithItem / isAttackable / getMainHandItem / ItemStack.getItem /
ItemBlock / BlockItem 及全部描述符）解析成一张 `JniMap` C++ 结构：

```
54 版本 × (1 vanilla + 1 forge + [1 mojang (1.14.4+)] + [1 intermediary (1.14+)])
= 171 张表
```

### 3.2 解析四步

1. **形态判定**：查 `joined.srg` —— 有 `func_71410_x` 且无 `field_72313_a` → S2；
   有 `m_91087_` → S3；否则 S1
2. **类名解析**：按规范名候选（含全部历史改名点）在 `classes.csv` 查
3. **成员解析**：按**稳定名**（`func_71410_x`/`m_91087_` 等唯一名）查 `joined.srg`，
   一条 MD/FD 行同时给出混淆侧和 Searge 侧
4. **描述符合成**：全部签名由类名拼接（不依赖 CSV 里不可靠的 type 显示文本）

### 3.3 用法

```bash
python gen_maps.py --emit                          # 生成 C++ 表
python gen_maps.py --report --versions 1.16.5      # 人工核对某版本
python gen_maps.py --query 1.20.1 getInstance      # 跨命名空间查名
```

### 3.4 生成层踩过的坑（均已修复 + 真机复验）

| 坑 | 根因 | 修复 |
|---|---|---|
| 方法描述符缺 `()` | `mcSig` 生成了 `Lenn;` 而非 `()Lenn;` | 方法描述符统一 `()L...;` 合成 |
| Intermediary 撞名 | 混淆方法名会重载撞名（`getItem` 的 obf 名 `h` 不唯一） | 用稳定名（Searge 列）查表 |
| 版本后缀撞名 | `"121"`(1.21) 误匹配 `"1121"`(1.12.1) | 末尾完整数字段精确匹配 |

---

## 四、注入层：MCCombatStatusJni.dll

### 4.1 注入与附加

`injector.exe` 按窗口标题找 java/javaw 进程 → `CreateRemoteThread + LoadLibraryA`
注入 DLL。DLL 内 JniWorker 线程：等 `jvm.dll` → `JNI_GetCreatedJavaVMs` →
`AttachCurrentThread`。

### 4.2 版本检测（为什么读 classpath）

Forge/Fabric 运行时无混淆类名（已被反混淆/remap），无法用混淆名指纹定位版本。
统一方案：读 `System.getProperty("java.class.path")`（兜底 `java.library.path`），
游戏 jar 路径必然含版本：

```
...\versions\1.21.11\1.21.11.jar          → 1.21.11
...\versions\1.16.5-Forge_36.2.34\...     → 1.16.5
...\versions\1.21.11-Fabric 0.18.4\...    → 1.21.11
```

### 4.3 类加载器（双份类副本）

游戏类由自定义加载器加载（LaunchClassLoader 1.8~1.12 /
TransformingClassLoader 1.13+ / KnotClassLoader Fabric）。用 JNI 的 `FindClass`
会拿到**静态字段未初始化的副本**（getMinecraft 返回 null）。解法：
Launch.classLoader 静态字段 → 线程遍历 context loader → 系统加载器兜底；
加载时 **findLoadedClass 优先、forName 兜底**；解析成功后**调 getMinecraft 验证
非 null**（副本判定法）。

### 4.4 判定逻辑

```
canAttack = 命中实体 && 是活物 && 存活 && 非自己
         && (canAttackWithItem() || 版本无此检查)   // 1.8~1.16.5
         && (isAttackable()  || 版本无此检查)        // 1.17+
canPlace  = 手持非空 && getItem() instanceof ItemBlock/BlockItem
```

---

## 五、上报层：输出协议

| 通道 | 内容 | 说明 |
|---|---|---|
| **共享内存** `Local\MCCombatStatus_<pid>` | 640 字节 `CombatStatus` 结构（含 canAttack/canPlace/targetName/map/env/tick 等） | 主通道；magic `'MCST'` + version 7 |
| **UDP** `127.0.0.1:35785` | 每 5ms 一个 **2 字节**包 `[canAttack '1'/'0'][canPlace '1'/'0']` | byte0 与旧版 1 字节协议兼容 |

完整 640 字节偏移表见 `D:\VibeCoding\MCCombatStatusToolkit\docs\04-消费端对接协议.md`。

---

## 六、消费层：连点器对接

`D:\VibeCoding\AutoClicker-main\` 是完整消费端参考实现（`canattack.cpp`）：

- **双通道**：共享内存轮询为主（常驻），UDP 兜底（门控开启才绑端口）
- **门控语义**：`仅能攻击时连点`=左键只在 `canAttack==1` 下发；`仅手持放置物时右键连点`=右键只在 `canPlace==1` 下发
- **fail-safe**：无新数据 300ms → 状态归 0；tick 必须递增；magic/version 校验
- **DLL 分发**：新 DLL 作为 RCDATA 资源嵌入 exe，启动时提取注入（V64 已重嵌 + 重构建）

---

## 七、命名体系与真机结论（知识核心）

### 7.1 三种运行时形态（判定 API 演变）

| 形态 | 版本 | 判定 API |
|---|---|---|
| **S1 字段式** | 1.8.8~1.13.2 | `typeOfHit`/`entityHit` 是字段（`field_72313_a`/`field_72308_g`） |
| **S2 getter 式** | 1.14~1.16.5 | HitResult 类族拆分，改 `getType()`/`getEntity()`（`func_216346_c`/`func_216348_a`） |
| **S3 getter 式** | 1.17~1.21.11 | 同 S2；映射体系换 stable `m_6662_`/`m_82443_` |

### 7.2 各环境运行时命名（真机实测）

| 环境 | 类名 | 成员名 | 加载器 |
|---|---|---|---|
| 原版启动器 | 混淆名 | 混淆名 | AppClassLoader |
| Forge 1.8~1.16.5 | MCP 名 | SRG `func_/field_` | LaunchClassLoader(1.8~1.12) / **TransformingClassLoader(1.13~1.16.5)** |
| Forge 1.17~1.20.1 | Mojang 名 | stable `m_/f_` | TransformingClassLoader |
| NeoForge 1.20.2+ | Mojang 名 | **全 Mojang 名** | TransformingClassLoader |
| Fabric 1.14+ | Intermediary | Intermediary | KnotClassLoader |

### 7.3 关键边界

- **加载器边界在 1.13**（ModLauncher 取代 launchwrapper）
- **命名边界在 1.17**（MCP→Mojang、SRG→stable）
- **NeoForge 边界在 1.20.2 前后**（stable→全 Mojang；1.20.1 实测 stable，
  1.20.4 实测全 Mojang，精确切换在 1.20.2/1.20.3）

### 7.4 稳定名规律（数据实锤）

- `func_71410_x`（getMinecraft）：1.8.8 → 1.16.5 全程不变
- `m_91087_`（getInstance）：1.17 → 1.21.11 全程不变
- `func_70075_an`（canAttackWithItem）：1.8.8 → 1.16.5 存在，1.17 移除
- `func_70694_bm`（无参 getHeldItem）：仅 1.8.8/1.8.9；1.9 起 `func_184614_ca`
- 类改名点：MovingObjectPosition→RayTraceResult(1.9)、EntityLivingBase→LivingEntity(1.14)、
  EntityPlayerSP→ClientPlayerEntity(1.14)、ItemBlock→BlockItem(1.14)

### 7.5 Intermediary 名漂移（Fabric 专属坑）

Intermediary 名**并非完全跨版本稳定**：`ItemStack.getItem` 在 1.14 是
`method_7949`、1.16.5/1.21.11 是 `method_7909`；同一 ID 在不同版本还可能指不同方法。
因此必须：稳定名查表 + 精确版本定位。

### 7.6 真机验证矩阵（V64）

| 命名空间 | S1 | S2 | S3 |
|---|---|---|---|
| vanilla | ✅ 1.8.9/1.12.2 diff | ✅ 原版 1.14 真机 | ✅ 1.20.1 diff |
| forge | ✅ diff | ✅ Forge 1.16.5 真机 | ✅ 1.20.1 diff |
| mojang | — | — | ✅ NeoForge 1.20.4/1.21/1.21.11 真机 |
| intermediary | — | ✅ Fabric 1.16.5 真机 | ✅ Fabric 1.21.11 真机 |

真机实测 7 个环境；未真机实测项（低风险）：S1 的 1.9~1.13.2、原版 1.14~1.21.x 其余版本。

---

## 八、使用方法速查

```bat
:: ① 数据更新后重新生成（两处任选其一，同步修改）
python D:\VibeCoding\MCCombatStatusJni\tools\gen_maps.py --emit
python D:\VibeCoding\MCCombatStatusToolkit\generator\gen_maps.py --emit

:: ② 编译 DLL + 注入器
cd /d D:\VibeCoding\MCCombatStatusJni   & build.bat
cd /d D:\VibeCoding\MCCombatStatusToolkit\dll   & build.bat

:: ③ 启动游戏 → 注入 → 验证
D:\VibeCoding\MCCombatStatusJni\injector.exe
D:\VibeCoding\MCCombatStatusJni\verify\reader_v7.exe <PID>

:: ④ 消费（UDP 采样）
python D:\VibeCoding\MCCombatStatusToolkit\verify\udp_listen.py

:: 查名（取代 grep lzma/mcp_config）
python gen_maps.py --query 1.20.1 getInstance
```

---

## 九、维护与扩展

### 新版本出现时

1. 更新数据：跑 `D:\mappings-gh-pages\extract_mappings.py <新版本>`
2. 若类名有新改名点：往 `gen_maps.py` 的 `CLASS_CANDIDATES` 加候选
3. `--emit` 重新生成 → 编译 → 真机验证（**假客户端测不出多加载器问题，真机不可替代**）

### 新增检测项（血量/攻击冷却/坐标等）

数据里每个字段/方法都查得到：`gen_maps.py --query <版本> <名字>` 找各命名空间名 →
`resolve()` 加稳定名 key → `JniMap` 结构加字段 → DLL 的 `ResolveWith`/`UpdateStatus`
加读取与判定。建议新成员按"可选解析"设计（失败不影响 canAttack）。

---

## 附录：全链路真机验证时揪出的 bug 清单

| # | bug | 修复 |
|---|---|---|
| 1 | `mcSig`/`heldItemSig`/`itemGetItemSig` 缺 `()`（方法描述符写成字段描述符） | 生成器统一 `()L...;` 合成 |
| 2 | Intermediary 撞名（混淆名查表不唯一） | 稳定名查表 |
| 3 | 版本后缀撞名（`121`↔`1121`） | 末尾数字段精确匹配 |
| 4 | 版本检测读窗口标题（标题可被改写/语言变化） | 改读 classpath |
| 5 | 解析失败后 mapIdx 不重置（永不重试） | 每轮从版本指纹重置 |
| 6 | DetectEnv 不认 NeoForge（FML 包名不同） | 加 `net/neoforged/fml` 标记 |
| 7 | 生成器 emit 打印表数错误、头注释漏命名空间 | 修复计数与注释 |
