# MC 多版本反混淆项目 · 技术文档

> **纯数据视角**的全链路技术文档：只讲本项目（`mappings-extracted`）自身——
> 数据从哪来、怎么提取转换、如何校验、产出什么、怎么查，
> 以及提取过程中踩过的坑、真实运行时核对记录与统计记录。
> 不涉及任何下游消费项目。

---

## 一、项目定位与数据规模

本项目是一份 **Minecraft Java 版多版本反混淆数据**：为 **1.8.8 ~ 1.21.11
共 54 个版本**，每个版本提供最多 **6 个命名空间**（混淆名 / Searge / Spigot /
Mojang / Yarn / Intermediary）的类、字段、方法、参数名映射。

| 指标 | 数值 |
|---|---|
| 版本数 | 54（53 个来自 takenaka 网站 + 1.8.9 来自官方 MCP 数据） |
| 类映射 | 约 29.9 万条 |
| 字段映射 | 约 127 万条 |
| 方法映射 | 约 202 万条 |
| 参数名 | 约 1081 万条 |
| 总数据量 | 约 1.7GB |

核心价值一句话：**"某个混淆名/成员名在任意版本、任意命名空间叫什么"，
从"逐个版本解压 lzma / 翻 mcp_config"变成了一次 grep 或一行 CSV 查询。**

---

## 二、全链路（本项目内部的完整流程）

```
┌────────────── 来源 A ──────────────┐   ┌────────────── 来源 B ──────────────┐
│ takenaka 反混淆网站 (53 个版本)      │   │ 官方 MCP 文件 (仅 1.8.9)           │
│  class-index.js + 每类一个 HTML 页  │   │  joined.srg/exc + fields/methods/  │
└──────────────────┬─────────────────┘   │  params.csv                        │
                   │                     └──────────────────┬─────────────────┘
                   ▼                                        ▼
        extract_mappings.py                     convert_mcp_189.py
        (HTML 解析 + 描述符重建)                 (MCP 格式 → 统一格式)
                   └──────────────────┬─────────────────┘
                                      ▼
                            统一格式 (每版本 7 个文件)
                            classes/fields/methods/params.csv
                            + joined.srg + joined.exc + mapping.json
                                      │
                                      ▼
                            校验 (见 §2.4)
                                      │
                                      ▼
                            查询 (grep / CSV)
```

### 2.1 来源 A：takenaka 网站（53 个版本）

- 站点结构：`<版本>/class-index.js`（全部类的 6 命名空间索引，tab 分隔）
  + `net/.../Class.html`（每个类一个页面，含类名表 + 字段/枚举/构造函数/方法表）
- 网页中每个名字按命名空间着色，CSS 类名与命名空间一一对应：
  `BS=混淆的 BQ=Searge BR=Spigot BN=Mojang BO=Intermediary BP=Yarn`
- 提取器据此从 HTML 的着色标记中恢复每个名字属于哪个命名空间

### 2.2 来源 B：官方 MCP 文件（仅 1.8.9）

takenaka 网站不收录 1.8.9，因此用官方 MCP 数据转换补全：

| MCP 文件 | 内容 | 转换后去处 |
|---|---|---|
| `joined.srg` | obf → Searge 的 CL/FD/MD 行 | 原样拷贝为 `joined.srg`（含 PK 行） |
| `joined.exc` | Searge 方法 → p_xxx 参数名 | 原样拷贝为 `joined.exc` |
| `fields.csv` | field_xxx → 可读名/side/desc | `fields.csv`（多 side/desc 两列） |
| `methods.csv` | func_xxx → 可读名/side/desc | `methods.csv`（多 side/desc 两列） |
| `params.csv` | p_xxx → 可读名/side | `params.csv`（多 MCP 命名空间行） |

1.8.9 的命名空间填充：`混淆的`=SRG 左侧混淆名、`Searge`=MCP 全限定名/`field_xxx`/`func_xxx`、
`Spigot`=**MCP 可读名**（该版本无 CraftBukkit 数据，此列被借用来存放可读名）、其余为空。

### 2.3 提取器的核心难点：JVM 描述符重建

网站页面只显示**可读的类型文本**（如 `Map<String, List<Entity>>`），而 `joined.srg`
需要 **JVM 描述符**（如 `(Ljava/util/Map;)V`）。提取器要做三步转换：

1. **泛型擦除**：`List<Entity>` → `List`；类型变量（单个大写字母）→ `Object`
2. **简单名 → 全限定名**：页面上的裸类型名（`Object`/`String`/`Stream`/`ImmutableMap`…）
   通过内置 100+ 条目的 `JDK_SIMPLE` 表映射到全路径；可点击的类型则从链接 href 取路径
3. **数组与 varargs**：`...` 与 `[]` 都按数组处理；原始类型用 `PRIMITIVES` 表
   （`int`→`I`、`boolean`→`Z`…）

每条方法输出**两套描述符**：混淆命名空间类型名一套、Searge 命名空间类型名一套
（与官方 MCP joined.srg 的两侧描述符惯例一致）。

> 重建失败的情况见「踩坑 §5.1」——现代版本约 0.02%（每 6 万方法约 2 个），
> 全部是网站渲染缺陷（返回类型单元格为空）导致的桥接方法。

### 2.4 校验

- **1.8.9 与官方 MCP 参考逐行核对**：`joined.srg` 的 CL 行 **100% 一致**；
  FD/MD 行除两版本间真实差异外一致；`joined.srg`/`joined.exc` 与参考文件**逐字节一致**
- **网站版与 1.8.9 交叉核对**：1.8.8 与 1.8.9 的 CL 行 100% 一致（两版本混淆类名未变），
  验证了网站提取与 MCP 转换两条链路产出的兼容性
- **统计自检**：每个版本记录 `desc_fail`（描述符重建失败数）与 `page_errors`
  （页面解析错误数），全部汇总在 `mapping.json` 与 `_summary.json`

### 2.5 产出格式（每版本 7 个文件）

| 文件 | 格式 | 说明 |
|---|---|---|
| `mapping.json` | JSON | 版本元数据（命名空间、颜色、统计） |
| `classes.csv` | `混淆的,Searge,Spigot,Mojang,Yarn,Intermediary` | 类名映射，内嵌类用 `$` |
| `fields.csv` | `owner_obf,6 命名空间,kind,type[,...]` | 字段 + 枚举常量 |
| `methods.csv` | `owner_obf,6 命名空间,kind,signature,descriptor,return_type[,...]` | 方法/构造函数 |
| `params.csv` | `owner_obf,member_obf,signature,kind,param_index,namespace,name` | 参数名长格式 |
| `joined.srg` | `CL:/FD:/MD:` 行 | obf → Searge（与官方 MCP 同格式） |
| `joined.exc` | MCP 参数名格式 | 尽力而为（旧版本近乎为空） |

### 2.6 查询方法

```bash
# SRG 行精确查询
grep '^MD: a/b ' mappings-extracted/1.8.8/joined.srg

# 类名跨命名空间
grep ',net/minecraft/world/entity/Entity' mappings-extracted/1.21.11/classes.csv

# 结构化查询 (推荐)
python - <<'EOF'
import csv
for r in csv.reader(open('mappings-extracted/1.21.11/methods.csv', encoding='utf-8-sig')):
    if r[0] == 'cgk' and 'setPose' in r[4]:   # r[4] 是 Mojang 列
        print(r[9])                            # JVM 描述符
EOF
```

---

## 三、六个命名空间详解

同一份 MC 代码在不同视角下有 6 套名字。CSV 列顺序**固定**为
`混淆的, Searge, Spigot, Mojang, Yarn, Intermediary`。

| 命名空间 | 含义 | 覆盖 | 示例（Minecraft 类 / getInstance） |
|---|---|---|---|
| 混淆的 | 官方 jar 真实混淆名 | 全部 54 版 | `ave`/`enn` / `A`/`N` |
| Searge | MCP/SRG：1.8~1.16.5 是 `func_/field_`，1.17+ 是 stable `m_/f_` | 全部 | `func_71410_x` → `m_91087_` |
| Spigot | CraftBukkit 名（1.8.9 例外：存 MCP 可读名） | 全部 | `getMinecraft` |
| Mojang | 官方映射 | 1.14.4+ | `net/minecraft/client/Minecraft` / `getInstance` |
| Yarn | Fabric Yarn | 1.14.4+ | `net/minecraft/client/Minecraft` / `getInstance` |
| Intermediary | Fabric 运行时（`class_/method_/field_`） | 1.14+ | `net/minecraft/class_310` / `method_1551` |

**命名空间覆盖的分界**：
- ≤1.13 的网站版只有前 3 列（混淆的/Searge/Spigot）
- 1.14.4 起才有 Mojang/Yarn/Intermediary（1.14~1.14.3 有部分）
- 1.17 起 Searge 列的内容从 `func_/field_` 换成 `m_/f_` stable 名（对应官方 MCP 换代）

---

## 四、版本分组与数据特征

| 组 | 版本 | 数据特征 |
|---|---|---|
| **旧网站版** | 1.8.8 ~ 1.13.2 | 仅 3 命名空间；参数名几乎全是 `argN` 占位（exc 行 = 0）；描述符重建 0 失败 |
| **过渡版** | 1.14 ~ 1.14.3 | 有 Intermediary/Yarn，无 Mojang；参数名从 1.14.4 才开始出现 |
| **现代版** | 1.14.4 ~ 1.21.11 | 6 命名空间齐全；参数名主要来自 Yarn；描述符重建偶有失败（1.20.4=4，1.20.6+=2） |
| **MCP 官方版** | 1.8.9 | 唯一由官方 MCP 转换的版本：有真实参数名（2838 条 exc）、side/desc 列、构造函数（1946 个） |

---

## 五、踩过的坑（数据侧）

### 5.1 网站渲染缺陷 → 描述符重建失败

**现象**：现代版本（1.20.4 起）出现少量方法 `descriptor` 列为空。
**根因**：网站对个别**桥接方法**的返回类型单元格渲染为空，无法重建描述符。
**现状**：1.20.4 有 4 个、1.20.6 起恒为 2 个（约 0.02%），`mapping.json` 的
`desc_fail` 字段记录；对应方法的 `joined.srg` MD 行缺失。
**教训**：数据源是"渲染后的网页"，不是原始数据——渲染缺陷会渗入产出，必须计数统计而非假设无损。

### 5.2 泛型擦除与 JDK 类名补全

网页显示的 `Map<String, List<Entity>>` 不是 JVM 描述符；类型变量、数组、varargs、
裸 JDK 类名（`Stream`/`ImmutableMap`/`Optional`…）都需要专门处理（见 §2.3）。
裸类型名靠 `JDK_SIMPLE` 表补全路径——**表里没有的类型就会重建失败**。

### 5.3 `access$` 合成方法缺失

官方 MCP 1.8.9 的参考 `joined.srg` 含约 **280 条 `access$N` 合成方法**
（如 `PlayerProfileCache$ProfileEntry/access$200`），takenaka 网站不收录这些，
故网站提取的版本里都没有。这是**源数据差异，不是解析错误**。

### 5.4 旧版本参数名缺失

网站版 ≤1.13 的参数名全是 `argN` 占位（`joined.exc` 为 0 行）；
真实参数名主要来自 Yarn（1.14.4+）与 Mojang（1.21+ 部分方法）。
唯一例外是 1.8.9（官方 MCP 数据自带真实参数名）。

### 5.5 1.8.9 与网站版的格式不统一

两个来源产出的"同名 CSV"结构并不完全一致：
- 1.8.9 的 fields/methods CSV **末尾多 `side`/`desc` 两列**（网站版无）
- 1.8.9 的 `Spigot` 列存的是 **MCP 可读名**（不是 CraftBukkit 名）
- 1.8.9 的 `joined.srg` 含 **7 条 `PK:` 行**（网站版混淆名全扁平、无包结构）
- 1.8.9 的构造函数来自 `joined.exc` 的 `<init>` 条目（网站版来自页面）
**教训**：统一格式 ≠ 完全同构，使用者必须按版本处理差异（列数、PK 行、命名空间语义）。

### 5.6 枚举常量的 kind 差异

1.8.9（MCP 转换）里枚举常量 `kind=field`；网站版里 `kind=enum`。
按 kind 列过滤枚举常量的代码会漏掉 1.8.9，必须按名字/位置判断而非 kind 列。

### 5.7 方法名带 `(...)` 后缀

methods.csv 的 Searge/Mojang/Yarn/Intermediary 列是 `name(args)` 显示形式
（如 `m_91087_()`、`getInstance()`），程序化使用时需要去掉尾部 `(...)` 才是真实成员名；
1.8.9 的混淆列还带参数类型（`a(int arg0)`），与网站版（`N()`）格式不同。

### 5.8 混淆方法名会重载撞名

混淆命名空间里重载方法共享同一个短名（如 `getItem` 的混淆名 `h` 在同类里不唯一）。
按"混淆名"查表不可靠，必须按**唯一标识**（Searge 稳定名 `m_41720_`/`func_xxx`）定位。

### 5.9 Intermediary 名跨版本漂移

Fabric 的 Intermediary 名**并非完全跨版本稳定**：实测 `ItemStack.getItem`
在 1.14 是 `method_7949`、1.16.5/1.21.11 是 `method_7909`；同一 ID 在不同版本
还可能指代不同方法（方法增删后 ID 复用）。**跨版本使用 Intermediary 数据必须按版本取。**

### 5.10 版本号后缀撞名

版本"1.21"与"1.12.1"去掉点后是 `121` 与 `1121`——后者以前者结尾。
任何按后缀匹配版本号的代码都会把 `121` 误配到 `1121`。必须按**完整数字段**精确比较。

### 5.11 编码与杂项

- 所有 CSV 带 BOM，须用 `utf-8-sig` 读取
- 版本目录里混有 `.git` 目录，遍历版本时须排除非版本目录
- `params.csv` 同一参数最多 3 行（混淆的/Searge/MCP 各一行），聚合时按
  `param_index` + `namespace` 去重

---

## 六、实机验证记录（数据 vs 真实运行时）

> 以下结论通过把本数据的各命名空间与**真实游戏运行时**（原版启动器、Forge、
> NeoForge、Fabric）实际加载的类/成员名逐一核对得出。核对手法：
> 注入后反射枚举运行时类/方法名，与 CSV 各列比对。

### 6.1 各环境运行时使用哪个命名空间（实测）

| 环境 | 运行时类名 | 运行时成员名 | 对应命名空间（列） |
|---|---|---|---|
| 原版启动器 | 混淆名 | 混淆名 | 混淆的 |
| Forge 1.8 ~ 1.16.5 | MCP 名 | SRG `func_/field_` | Searge |
| Forge 1.17 ~ 1.20.1 | Mojang 名 | stable `m_/f_` | Mojang(类) + Searge(成员) |
| NeoForge 1.20.2+ | Mojang 名 | Mojang 官方名 | Mojang |
| Fabric 1.14+ | Intermediary | Intermediary | Intermediary |

### 6.2 稳定名跨版本规律（数据实锤）

- `func_71410_x`（getMinecraft）：**1.8.8 → 1.16.5 全程不变**
- `m_91087_`（getInstance）：**1.17 → 1.21.11 全程不变**
- `func_70075_an`（canAttackWithItem）：1.8.8 → 1.16.5 存在，1.17 移除
- `func_70694_bm`（无参 getHeldItem）：**仅 1.8.8/1.8.9**；1.9 起换 `func_184614_ca`
- 类改名点：`MovingObjectPosition`→`RayTraceResult`(1.9)、
  `EntityLivingBase`→`LivingEntity`(1.14)、`EntityPlayerSP`→`ClientPlayerEntity`(1.14)、
  `ItemBlock`→`BlockItem`(1.14)

### 6.3 关键边界

- **加载器换代在 1.13**（ModLauncher 取代 launchwrapper）
- **命名换代在 1.17**（MCP→Mojang 类名、SRG→stable 成员名）
- **NeoForge 换代在 1.20.2 前后**（stable 成员 → 全 Mojang；1.20.1 实测 stable、
  1.20.4 实测全 Mojang，精确切换在 1.20.2/1.20.3）

### 6.4 真机验证矩阵

| 命名空间 | 1.8~1.13.2 | 1.14~1.16.5 | 1.17+ |
|---|---|---|---|
| 混淆的 | ✅ 1.8.9/1.12.2 | ✅ 原版 1.14 | ✅ 1.20.1 |
| Searge | ✅ 1.8.9/1.12.2 | ✅ Forge 1.16.5 | ✅ 1.20.1 |
| Mojang | — | — | ✅ NeoForge 1.20.4/1.21/1.21.11 |
| Intermediary | — | ✅ Fabric 1.16.5 | ✅ Fabric 1.21.11 |

---

## 七、提取记录

### 7.1 时间线

- **2026-08-12 ~ 2026-08-13**：完成 53 个网站版本提取（多进程并行，单版本 12~61 秒）
  + 1.8.9 官方 MCP 转换

### 7.2 提取统计摘要（节选自 extraction_log.txt）

| 指标 | 旧版（1.8.8） | 中期（1.14.4） | 现代（1.21.11） |
|---|---|---|---|
| 类 | 2451 | 4721 | 9605 |
| 字段 | 8255 | 17277 | 43885 |
| 方法 | 17991 | 30857 | 63305 |
| 参数行 | 48450 | 187068 | 357070 |
| SRG 行 | 25937 | 47985 | 107299 |
| EXC 行（真实参数名） | 0 | 8761 | 33497 |
| 描述符重建失败 | 0 | 0 | 2 |

规律：类/方法数量随版本单调增长；真实参数名从 1.14.4 开始出现（Yarn 源）；
描述符重建失败仅在 1.20.4+ 出现（且恒为个位数）。

### 7.3 数据完整性指标

| 指标 | 值 | 说明 |
|---|---|---|
| 页面解析错误 (page_errors) | 全版本 0 | 无页面抓取失败 |
| 描述符重建失败 (desc_fail) | 仅 1.20.4=4，1.20.6+ 恒 2 | 网站渲染缺陷所致（§5.1） |
| 1.8.9 CL 行与官方 MCP 一致性 | 100% | 逐行核对 |
| 1.8.9 SRG/EXC 与参考逐字节一致性 | 100% | 原样拷贝 |

---

## 八、已知限制清单

1. **网站版（除 1.8.9）无 MCP 风格参数名**：参数名几乎全是 `argN` 占位；
   真实参数名靠 Yarn（1.14.4+）与 Mojang（1.21+ 部分）
2. **`access$` 合成方法缺失**：网站不收录（约 280 条，仅 1.8.9 参考文件有）
3. **描述符重建失败**：现代版本约 0.02%（2~4 个桥接方法），对应 SRG MD 行缺失
4. **1.8.9 与网站版结构差异**：多 `side`/`desc` 列、Spigot 列语义不同、含 PK 行（§5.5）
5. **无 1.8.9 以外的官方 MCP 版本**：≤1.13 只有 3 命名空间，没有 side/desc/真实参数名
6. **Intermediary 名跨版本漂移**：跨版本使用必须按版本取（§5.9）
